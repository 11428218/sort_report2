# 排序報告

學號：11428218

姓名：蔡名瑜

模擬頁面：[[點此進入](index.html)](https://11428218.github.io/sort_report2/)

---

## 專題說明

本專題以三個常見排序演算法為主，製作成可互動的視覺化網頁，讓使用者能直接觀察排序過程中的比較、交換與資料位移情形。

目前完成的頁面如下：

1. [首頁導覽](index.html)
2. [Bubble Sort 視覺化](bubble-sort.html)
3. [Selection Sort 視覺化](selection-sort.html)
4. [Insertion Sort 視覺化](insertion-sort.html)

---

## 首頁導覽

首頁採卡片式版面，提供三個排序頁面的入口：

1. 前往 Bubble Sort 頁面
2. 前往 Selection Sort 頁面
3. 前往 Insertion Sort 頁面

這個頁面主要功能是集中導覽，讓報告閱讀者可以快速切換到各排序演算法的實作頁。

---

## 氣泡排序法（Bubble Sort）

### 原理簡述

氣泡排序法會重複走訪陣列，依序比較相鄰兩個元素，若順序錯誤就交換。每完成一輪，當前最大值會被推到右側正確位置。

### 流程重點

1. 從索引 0 開始比較相鄰元素。
2. 若左值大於右值則交換。
3. 一輪結束後，最右側元素固定。
4. 若某輪沒有交換，可提前結束。

### 複雜度分析

- 最佳時間複雜度：O(n)
- 平均時間複雜度：O(n^2)
- 最差時間複雜度：O(n^2)
- 空間複雜度：O(1)
- 穩定性：穩定排序

### 模擬實作內容

Bubble Sort 頁面提供輸入陣列、隨機產生、速度控制、開始排序、停止與單步執行等功能，能清楚觀察每一次比較與交換。

---

## 選擇排序法（Selection Sort）

### 原理簡述

選擇排序每一輪從未排序區間中找出最小值，與當前輪起始位置交換，使左側區間逐步成為已排序狀態。

### 流程重點

1. 第 i 輪先假設第 i 個元素是最小值。
2. 掃描 i 到 n-1，找出真正最小值索引。
3. 將最小值與 i 位置交換。
4. i 往右推進，直到排序完成。

### 複雜度分析

- 最佳時間複雜度：O(n^2)
- 平均時間複雜度：O(n^2)
- 最差時間複雜度：O(n^2)
- 空間複雜度：O(1)
- 穩定性：不穩定排序

### 模擬實作內容

Selection Sort 頁面可即時顯示當前輪次、比較次數、交換次數與目前最小值位置，適合觀察每輪只做一次交換的特性。

---

## 插入排序法（Insertion Sort）

### 原理簡述

插入排序將陣列分成已排序區與未排序區，每次取未排序區第一個元素，插入到已排序區中的正確位置。

### 流程重點

1. 將第一個元素視為已排序。
2. 從第二個元素開始，記錄當前值 key。
3. 向左比較已排序區元素，較大者右移。
4. 找到位置後放入 key。

### 複雜度分析

- 最佳時間複雜度：O(n)
- 平均時間複雜度：O(n^2)
- 最差時間複雜度：O(n^2)
- 空間複雜度：O(1)
- 穩定性：穩定排序

### 模擬實作內容

Insertion Sort 頁面除了基本的開始、暫停、單步與重置外，也能調整資料筆數與速度。畫面會同步顯示比較次數、移動次數與目前狀態。

---

## 綜合比較

| 演算法 | 最佳時間 | 平均時間 | 最差時間 | 空間 | 穩定性 | 特性摘要 |
|---|---|---|---|---|---|---|
| Bubble Sort | O(n) | O(n^2) | O(n^2) | O(1) | 穩定 | 實作簡單，適合教學 |
| Selection Sort | O(n^2) | O(n^2) | O(n^2) | O(1) | 不穩定 | 比較次數固定，交換次數少 |
| Insertion Sort | O(n) | O(n^2) | O(n^2) | O(1) | 穩定 | 近乎有序資料表現佳 |

### 使用建議

- 小型或近乎有序資料：Insertion Sort
- 教學與基礎理解：Bubble Sort、Selection Sort
- 若要更進一步，可延伸到 Merge Sort 或 Quick Sort 的分治法概念

---

## 學習心得

這次我用AI幫我寫網頁頁面，我既體會到AI的便捷也更深入了解AI的更多功用。但有時候AI也會不知道我想要表達什麼有可能是我還不太會下指令，我需要更努力練習如何應用AI與如何下指令。
